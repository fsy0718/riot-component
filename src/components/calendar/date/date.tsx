import * as React from "react";
import * as ReactDOM from "react-dom";

interface CalendarDateProps {
    d: Date
}

export default class extends React.Component<CalendarDateProps, any>{
    constructor(props){
        super(props);
        
    }
}